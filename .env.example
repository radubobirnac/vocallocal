# OpenAI API key for transcription and translation
OPENAI_API_KEY=your_openai_api_key_here

# Google Gemini API key for transcription and translation
GEMINI_API_KEY=your_gemini_api_key_here

# Secret key for Flask session security (will be auto-generated in production)
SECRET_KEY=your_secret_key_here

# Firebase Configuration
FIREBASE_DATABASE_URL=https://your-project-id.firebaseio.com
FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com

# Credential Files (Recommended Approach)
# Create these files directly on your server using console commands:
# - Oauth.json (Google OAuth credentials)
# - firebase-credentials.json (Firebase service account)
# See CONSOLE_CREDENTIAL_SETUP.md for detailed instructions

# Google OAuth credentials - Individual fields (alternative approach)
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here
GOOGLE_REDIRECT_URI=https://localhost:5001/auth/callback

# Advanced: Environment Variable Credentials (optional)
# Use these only if you prefer environment variables over credential files
# FIREBASE_CREDENTIALS_JSON={"type":"service_account","project_id":"..."}
# GOOGLE_OAUTH_CREDENTIALS_JSON={"web":{"client_id":"...","client_secret":"..."}}

# Admin user credentials (for initial setup)
ADMIN_USERNAME=admin
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change_this_password
FIREBASE_STORAGE_BUCKET=vocal-local-e1e70.appspot.com
